import { STORAGE, URL, API } from './config'
import images  from './images';
import theme, { COLORS, SIZES, FONTS } from "./theme";

export { theme, COLORS, SIZES, FONTS, images, STORAGE, URL, API };
